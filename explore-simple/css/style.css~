/* General body styling */
body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    background-color: #f0f0f0;
}

/* Game container (the large box) */
#game-container {
    width: 600px;
    height: 600px;
    background-color: #ffffff;
    border: 2px solid #000000;
    position: relative;
    overflow: hidden;
}

/* Player dot */
/* Player sprite */
#player {
    width: 32px; /* Match the sprite dimensions */
    height: 32px;
    background: url("../images/player.png") no-repeat center center;
    background-size: cover;
}

/* Interactive white dots */
.interactive-dot {
    width: 20px;
    height: 20px;
    background-color: white;
    border: 2px solid black;
    border-radius: 50%;
    position: absolute;
}

